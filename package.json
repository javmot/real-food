{
  "name": "@real-food/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @real-food/theme-ui build && lerna run dev --parallel",
    "lint": "lerna run lint --parallel"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.21.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run pre-commit"
    }
  }
}
